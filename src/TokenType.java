public enum TokenType {
    LPAREN,
    RPAREN,
    LBRACKET,
    RBRACKET,
    LBRACE,
    RBRACE,
    COMMA,
    SEMICOLON,
    DOT,
    COLON,

    // keywords
    AS,
    DECLARE,
    IF,
    ELSE,
    TRUE,
    FALSE,
    NULL,
    RETURN,
    MATCH,
    CASE,
    OTHERWISE,
    NEW,
    PRINT,
    RELEASE,
    DEFER,
    END,

    // Iterator keywords
    WHILE,
    REPEAT,
    UNTIL,
    FOR,
    TO,
    STEP,
    IMPORT,
    MODULE,
    EXIT,
    LOOP,

    // Literals
    NUMBER,
    STRING,
    IDENTIFIER,

    // Operators
    SIMPLE_ASSIGN,
    COMPLEX_ASSIGN,
    REL_OPE,
    EQU_OPE,
    TERM,
    FACTOR,
    LOGICAL_OR,
    LOGICAL_AND,
    LOGICAL_NOT,
    IGNORE,
    EOF,    
}
